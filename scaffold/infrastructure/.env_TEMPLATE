# Copy to .env.dev and .env.prod and fill in values.
# These files are gitignored — never commit them.
# In CI, these values are set as CodeBuild environment variables by pipeline-stack.ts.

CDK_DEFAULT_ACCOUNT=
CDK_DEFAULT_REGION=
GITHUB_OWNER=
GITHUB_REPO=

# prod only
PROD_DOMAIN_NAME=
CERTIFICATE_ARN=
