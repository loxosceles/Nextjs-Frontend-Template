# Copy to .env.dev and .env.prod and fill in values.
# These files are gitignored — never commit them.
# In CI, these values are fetched from SSM by buildspec.yml.

CDK_DEFAULT_ACCOUNT=
CDK_DEFAULT_REGION=
AWS_REGION_DEFAULT=
GITHUB_OWNER=
GITHUB_REPO=

# prod only
PROD_DOMAIN_NAME=
CERTIFICATE_ARN=
