{
  "name": "infrastructure",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "ts-node lib/cli/bin/deploy.ts",
    "build-and-publish-web": "ts-node lib/cli/bin/build-and-publish-web.ts",
    "ssm-params": "ts-node lib/cli/bin/ssm-params.ts",
    "destroy:dev": "ENVIRONMENT=dev cdk destroy --all --force",
    "destroy:prod": "echo 'Production destroy not allowed' && exit 1",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "aws-cdk": "^2.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@aws-sdk/client-cloudfront": "^3.0.0",
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-ssm": "^3.0.0",
    "@aws-sdk/lib-storage": "^3.0.0",
    "aws-cdk-lib": "^2.0.0",
    "commander": "^12.0.0",
    "constructs": "^10.0.0",
    "dotenv": "^16.0.0"
  }
}
